apiVersion: v1
kind: Service
metadata:
  name: dashboard-service
  namespace: dashboards
  labels:
    app: dashboard
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: dashboard